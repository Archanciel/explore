# Next import instruction avoids importing the class in the
# kivycustomclassbuttonsexplore.py app python file, import
# which will be removed by Pycharm when optimizing the imports !

#: import KivyCustomClassButtons kivycustomclassbuttons.KivyCustomClassButtons

<KivyCustomClassButtons>: # inherits from BoxLayout
    id: custom_class_buttons_instance # the instance type will be the
                                      # lowest sub class of KivyCustomClassButtons.
    orientation: 'vertical'
    Button:
        id: first_button
        text: "First Button"
        on_press: custom_class_buttons_instance.firstButtonPressed()
    Button:
        id: second_button
        text: "Second Button"
        on_press: custom_class_buttons_instance.secondButtonPressed()
