# Next import instruction avoids importing the class in the app python file, import
# which will be removed by Pycharm when optimizing the import !
#: import KivyCustomClassButtons kivycustomclassbuttons.KivyCustomClassButtons

<KivyCustomClassButtons>: # inherits from BoxLayout
    orientation: 'vertical'
    Button:
        id: first_button
        text: "First Button"
        on_press: root.firstButton()
    Button:
        id: previous_track
        text: "Second Button"
        on_press: root.secondButton()
