# Next import instruction avoids importing the class in the app python file, import
# which will be removed by Pycharm when optimizing the import !
#: import KivyCustomClassGUI kivycustomclassgui.KivyCustomClassGUI

<KivyCustomClassGUI>:
	canvas:
		Color:
			rgba: 0.3, 0.3, 0.3, 1
		Rectangle:
			size: self.size
			pos: self.pos
	orientation: 'vertical'
	BoxLayout:
		orientation: 'vertical'
		BoxLayout:
			Button:
				id: next_track
				text: "Next Track"
				on_press: root.nextTrack()
			Button:
				id: previous_track
				text: "Previous Track"
				on_press: root.previousTrack()
